// Product Types
export enum PRODUCTS_TYPES {
  PRODUCT = "product",
  SEMI_FINISHED = "semi_finished",
  CONTAINER = "container",
  FINISHED_GOOD = "finished_good",
  INGREDIENT = "ingredient",
}

// Stock Adjustment Types
export enum ADJUSTMENT_TYPES {
  IN = "in",
  OUT = "out",
  ADJUSTMENT = "adjustment",
}

// Stock Movement Directions
export enum MOVEMENT_DIRECTIONS {
  IN = "in",
  OUT = "out",
}
